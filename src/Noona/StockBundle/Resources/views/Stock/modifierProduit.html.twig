{% extends ':Layout:layout.html.twig' %}

{% block content %}

<h2>Modification de produits</h2>

    <a href="{{ path('noonastock_effacerProduit',{'id':form.vars.value.id}) }}" class="btn btn-primary pull-right" onclick="return confirm('Cette action est irréversible. Tu es sure?')">Supprimer fiche produit</a>
    <img src="{{ asset(form.vars.data.image.webpath) }}" alt=""/>

    {{ form_start(form) }}
    <div class="alert-warning">{{ form_errors(form) }}</div>


    <div class="form-group">
        <div class="control-label">{{ form_label(form.reference) }}</div>
        {{ form_errors(form.reference) }}
        {{ form_widget(form.reference) }}
    </div>




    <div class="form-group">
        <div class="control-label">{{ form_label(form.image) }}</div>
        {{ form_errors(form.image) }}
        {{ form_widget(form.image) }}
    </div>



    <input class="btn btn-primary" type="submit" value="Envoyez le formulaire"/>
    {{ form_end(form) }}






{% endblock %}


